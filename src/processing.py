def filter_by_state(loc_dict: dict, state = 'EXECUTED') -> list[dict]:
    new_list = []
    another_list = []
    if 'state':
        new_list = new_list.append(loc_dict)
    else:
        another_list = another_list.append(loc_dict)

    return new_list, another_list